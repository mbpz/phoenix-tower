# macOS 默认启动可玩性修复

2026-09-06；基线 `74b710c`。用户提供默认 cargo run 全窗口截图，文字溢出、控件重叠、HUD 遮挡。此前离屏 3D 截图和正尺寸日志不构成交互验收，本轮不沿用该结论。

## 修改前计划

1. 主执行检查 Lunex 的测量/缩放同步、文字边界及布局；先补失败回归，再作应用层修复。不改 registry、不升级依赖、不改变存档。
2. 独立调查 UI 与游戏输入隔离、点击/拖动/滚轮和撤销快捷键。具体缺陷确认后补回归修复。
3. 简化常驻 HUD：保留当前操作与教程，完整快捷键按需展开，防止长文本遮挡主要操作区域。
4. 默认模式原生全窗口验证（非 riverside 特例、非离屏截图）：可读性、四个标签、积木选择、放置/撤销/旋转、滚轮和不同窗口大小。可用工具不能覆盖的行为明确记录，不以模拟输入状态冒充人工操作。
5. 完整测试、check、Clippy、fmt、独立复核；每次视觉迭代保存真实窗口证据及 verdict。

## 本轮实现与回归（2026-09-06）

- 文本：移除 Text2d 标签的 `full()` 父级相对尺寸，统一固定字号；补充测量变化后的缩放同步，避免 Retina 字体测量与布局尺寸叠加。回归直接调用锁定版 Lunex 的测量系统。
- 标签页：列表行恢复 `Inherited` 可见性，不再穿透隐藏父级；选中积木改变时滚入可见区域，同一选择不抢回用户手动滚动。
- 点击：应用层拾取只使用专属正交 UI 相机；完整布局根不拦截全窗口点击。保留锁定版的深度、变换、可见性及阻挡语义，不修改依赖源码。
- 输入：同一帧共享 UI 所有权，鼠标手势从按下起锁定归属，拖动超过阈值后返回原点也不视为放置。UI 滚轮不缩放场景；文本输入捕获游戏快捷键，切页/外部点击释放焦点；Cmd+Shift+Z 重做不同时撤销。
- HUD：常驻内容缩减，完整帮助改为 F1 展开；移除空状态行；中文允许逐字换行。主题按钮与透明度滑杆重新分离。
- 多相机合成：3D 相机显式匹配 2D 相机 `Msaa::Off`，避免 Bevy 按不同采样数分配独立颜色纹理。此修复有组件回归，仍需原生画面证明。
- 简化：删除旧的独立点击判定状态，集中输入归属；移除本轮临时全 UI 离屏截图逻辑，不引入新依赖、不改变存档协议。

## 已执行验证

- `cargo test --locked --offline -j 2`：129 通过，0 失败，1 忽略。
- `python3 -B -m unittest discover -s tools -p 'test_*.py' -v`：40 通过。
- `cargo fmt --all --check`：通过。
- `cargo clippy --locked --offline --all-targets -j 2 -- -D warnings`：通过。
- `cargo build --locked --offline -j 2`：默认 dev 配置构建通过，与默认 `cargo run` 使用相同配置。
- `git diff --check`：通过。
- 独立只读复核指出输入框点击外部后焦点不释放，已修复并加入 7 项输入归属回归。测试中的 HoverMap 是注入状态，不能等同原生事件链验收。

## 原生验收状态与剩余风险

Computer Use 权限曾实际生效，已获取本机原生窗口截图：巨大文字重叠问题改善，文字与标签可读；但当时场景没有显示、点击标签无响应，并有透明度重叠及 HUD 空白。这些问题随后追加了上述修复，**尚未取得修复后的完整原生验收证据**。

最后尝试重新打开原生游戏时，工具明确返回 `The Mac is locked and automatic unlock could not unlock it`。这是锁屏阻塞，不是继续修改权限或改用另一种控制工具的理由。需用户手动解锁后继续：

1. 默认启动确认 3D 可见，且镜头构图、HUD 高度和主题/滑杆布局可用。
2. 点击四个标签，检查隐藏内容不串页；教程默认选择应出现在积木列表中。
3. 放置、撤销、Cmd+Shift+Z 重做、旋转；拖动镜头不能误放置。
4. 列表滚轮与场景缩放隔离，存档输入焦点捕获及外部点击恢复快捷键。
5. 调整窗口大小并检查 Retina 布局，重新记录 visual-verdict。

当前不宣称“游戏已可玩”，不以构建和单元测试替代以上验收。锁定依赖升级时必须重新核对拾取适配器与文字缩放同步。

## 原生解锁后第二轮计划与证据（2026-09-07）

- 已看到恢复的 3D 场景；原生鼠标进入标签造成进程退出，macOS 崩溃栈证实 Lunex 0.7 的 `UiHoverSet` 子级复制未改 target，形成无限递归。应用层直接更新悬停状态，避免发送这一上游事件；新增事件计数回归，先失败后通过。
- HUD 的百分比最大宽度在文字内在测量时再次约束宽度，造成 90px 文字撑出 204px 面板。用视口宽度单位替代父级百分比，真实 Taffy/字体测量回归覆盖 1x/2x 和 640/1280 宽度，已复现红灯。
- 后续改动前计划：仅在启动时按蓝图边界取景，保留玩家后续相机控制与 riverside 的覆盖；先写投影边界测试。重启本轮测试进程，逐项验证点击、输入隔离、放置与撤销，不把 CUA 连接失败直接判定为游戏死锁。
- 卡顿采样只有两次采样命中有界的 placement 扫描，不足以证明死锁；稍后的窗口已有旋转/跳过教程变化。因此不据此大改放置算法，先恢复原生控制连接再判断。

### 2026-09-07 提交检查点

- 最新全量 Rust 测试 **132 通过、1 忽略**；Clippy all-targets `-D warnings`、fmt、diff-check、dev build 均通过。Python 工具测试 40 通过。
- 默认镜头已按蓝图边界一次性取景；真实投影回归覆盖 1280×720、900×720，确认后续帧不会重置玩家平移。
- 原生已见：四个标签可切换，隐藏页不串页；输入 `rce` 未触发旋转/挑战/选块；HUD 缩紧，整座蓝图进入画面。
- **未验收完毕**：新镜头距离下雾效使蓝图对比度不足；放置/撤销/重做及连续拖动需继续原生验证。临时测试应用已改为实际二进制副本（与 dev 构建相同），避免符号链接包的窗口归属不稳定；不是发布安装包。
- 提交后继续计划：先用可量化回归保证建造区不受距离雾冲淡，再复验原生操作。此检查点不等于成品可玩性验收。

## 提交后的续修与实测（2026-09-07）

- 按用户要求，先将上述阶段修复提交为 `058541a`（未推送）。
- 后续在 `src/building/placement.rs` 将交互积木、蓝图与放置幽灵材质排除出距离雾；不关闭景观雾、不改变透明度、不引入依赖。新增真实材质初始化回归，先失败后通过。
- 完整复验：Rust 133 通过、0 失败、1 忽略；Clippy all-targets `-D warnings`、fmt、diff check 和默认 dev 构建通过。此前 Python 40 项通过；本次没有修改 Python。
- 重新启动相同 dev 二进制后取得图鉴、成就页实际切换画面。新鲜截图 HUD 瞬时 FPS 52–66，仅为观察值，不是持续性能基准。先前多次重复 FPS 1 画面不足以证明游戏卡死；5 秒进程采样也没有提供死锁证据。
- Computer Use 仍间歇返回 `noWindowsAvailable`，部分重新查询能恢复截图，但后续点击再次失败。放置、撤销、重做及拖动/滚轮隔离的完整原生链路仍未验收，不能把单测或工具调用成功当作实测通过。
- 视觉 verdict 保持 revise / 78：虽然文字溢出已修，默认远景下蓝图仍偏小且对比不足，不宣称达到可玩交付标准。运行日志还出现 ICU 中文/日文分词模型缺失警告，其影响尚未定性，未通过改依赖或关闭错误输出掩盖。

## 第三轮修改前计划（2026-09-07）

1. 锁定高处蓝图点击与占用排除回归。预览和提交共用射线目标查询；保留自由模式地面堆叠，复用 Bevy AABB 射线函数，不增加依赖。
2. 锁定教程撤销后返回未完成步骤的回归，避免自动选材把玩家锁在下一步。
3. 教程初始镜头聚焦三步施工区域，蓝图只显示当前步骤未放置的格子；退出教程恢复全图。提供 Home 全景复位，保留玩家平移/缩放控制。先补组件/投影回归，再改画面。
4. 编译后原生复测放置、撤销、重做、旋转、拖动、滚轮及存档输入隔离。独立实现子代理因 upstream_error 启动失败，本轮实现回到主执行，不声称独立复核通过。

### 第三轮原生中途验收与下一个修复计划

已实测台基点击：1 块 / 25% / 教程第 2 步；Backspace 撤销：0 块 / 0% / 第 1 步。当前步骤近景比上一轮更明确。Command-Z、Command-Shift-Z、Ctrl-Y 尚未观察到生效；CUA 仍间歇 noWindowsAvailable。先用回归验证一帧内按下并释放修饰键时，pressed-only 判定是否丢失组合键，再针对已复现缺陷修复；这只是输入失败的候选原因，不将其当作原生根因已确认。

只读子代理审查发现两项需修：射线可能穿过已放置前柱命中后柱；完成观赏仍沿用首层焦点导致塔顶出框。修复前增加遮挡射线与完成态投影回归；遮挡采用已有占用格边界（保守，不引入物理依赖），观赏复用同一包围盒取景计算。代理未执行原生验收或独立编译，不把其分析等同运行通过。

### 近景新增缺陷：网格原点不一致

原生已放置两根柱（3 块 / 29%），截图证实柱身悬空。代码核对：column 从 y=0 生成，却放在 block_center（再次加半柱高）；台基复用了 0.5 高的檐板，而占用 2 格，且檐板没有台基需要的完整顶面。先加真实渲染资产的世界高度回归，再将柱网格平移到中心原点、台基复用 Cuboid 实体尺寸，不改蓝图/存档坐标，不扩展到其他美术资产。

### 第三轮验收记录（2026-09-07，未全部通过）

- 高处放置的预览与点击提交共用射线目标；排除已占用目标及前方遮挡。自由模式保留地面堆叠行为。回归先失败后通过。
- 教程撤销回退到未完成步骤；仅展示当前步骤未放置蓝图；初始近景与 Home 全景复用边界取景，完成观赏重新按全塔边界取景。
- 组合键的一帧内释放处理已有回归修复，但原生 Command-Z / Command-Shift-Z 仍没有生效证据，不能宣称已解决。
- 网格高度测试先复现 datiji bottom=3（预期 2）；台基改为真实占用尺寸 Cuboid，建筑柱网格中心对齐。没有改蓝图坐标或新增依赖；灯具、屋顶等其他网格仍需另行审视。
- 最终本轮自动验证：Rust **143 通过、1 忽略、0 失败**；Clippy all-targets `-D warnings`、fmt、diff check、dev build 均通过；Python 工具测试 **40 通过**。
- 新二进制原生实测：台基 1 块 / 25%；首柱 2 块 / 27%；台基加四柱 **5 块 / 33%**，自动进入第 3 步，两根梁的蓝图出现。台基顶面完整、柱脚贴合台基，不再悬空。前一个修复版本还实测了 Backspace 撤销回到教程第 1 步；最终网格版本未重复该项。
- 随后 CUA 明确返回 Mac 已锁定且自动解锁失败。高处两梁完成、原生撤销重做组合键、拖动不误放、面板滚轮隔离、存档焦点恢复、窗口尺寸/Retina 复验仍待继续。保留当前临时游戏进程，不将锁屏或 CUA 的间歇窗口错误判为游戏崩溃。
- 视觉判定 revise / 85：布局和第一、二步已有可见改善，但场景仍偏暗、完整教程及输入验收尚未闭环。`058541a` 之后的这些修复尚未提交、未推送。

## 第四轮修改前计划（2026-09-07）

已按用户要求提交第三轮修复为 `084f270`，未推送。恢复 GUI 后窗口当前为空场景，重新从第一步验收；CUA 仍间歇找不到窗口，不复用旧进度当作当前结果。

- 本地继续原生点击/键盘链路；若工具送键与实际事件不一致，先取得事件证据，不再猜测修饰键根因。
- 独立检查相机滚轮的 Pixel/Line 单位：当前直接将 delta.y 当作格数，可能使 Mac 触控板缩放瞬间触底。先加系统回归，再仅修相机的单位归一化，保持 UI 所有权隔离；不加依赖、不改持久化数据。

### 第四轮结果（2026-09-07，验收仍开放）

- 相机单位回归先失败：等效负向滚轮输入 Line 得到距离 30.636879，Pixel 却跳到最大距离 160。修复时复用锁定 Bevy 的 `MouseScrollUnit::SCROLL_UNIT_CONVERSION_FACTOR`，仅将 Pixel 换算成 Line；不改行滚轮灵敏度、UI 输入所有权或相机距离边界。
- 该常量是近似换算，不是 Mac 触控板手感已经合格的证明；需后续原生滚动校准。本轮子代理因 upstream_error 未执行，实现与验证均回到主执行，未声称独立审核通过。
- 全量 Rust **144 通过、1 忽略**；Clippy all-targets `-D warnings`、fmt、diff check、dev build 通过。本轮未改 Python，沿用上一轮 40 项通过记录，未声称重新运行。
- 临时验收应用已重启至新构建。尝试路径与 bundle ID 连接、重置控制会话、窗口 Raise/标题栏/可访问性点击；能够看到原生窗口和前台状态，但动作仍间歇 `noWindowsAvailable`，本轮尚未取得新的放置或组合键生效证据。旧轮 5 块 / 33% 不能冒充当前运行状态。
- 未通过项仍为：高处梁教程闭环、Command 撤销重做、拖动/滚轮隔离、存档输入焦点、窗口尺寸/Retina 复验。当前不是锁屏阻塞，不能沿用上一轮“请解锁”的诊断；具体阻塞是 CUA 窗口动作不可靠。
- `084f270` 是本轮开始提交的检查点；后续滚轮修复和本记录留在工作区，未推送。

## 第五轮修改前计划（2026-09-07）

滚轮修复已提交为 `ea8fd3a`，未推送。本轮不再重复猜测组合键根因：增加显式环境变量启用的、只观察游戏输入的诊断日志，区分窗口焦点、输入是否到达、UI 捕获、最终积木计数。默认关闭，不输出输入框文本、存档路径或任意键名，不新增依赖。现有 144 项测试锁定输入所有权行为；诊断不参与输入决策。重启诊断版本后执行放置与普通键/组合键对照，再据证据修复。

## 第六轮修改前计划（2026-09-07）

身份改写后的检查点为 `8d4f3c4`；用户告知已推送，不再改写历史。当前输入日志实证 Backspace 使 1→0，重新台基及四柱使 0→5；CUA 部分截图仍显示旧的 1 块画面，不能视为实时渲染证据。

- 主线：使用已有 F2 游戏内截图交叉验证实时画面，继续两梁、拖动、滚轮与焦点隔离，不根据陈旧截图修改正确逻辑。
- 独立小修：先补中英文帮助的快捷键回归，再替换缺字的 Command/Shift 符号为 ASCII 名称，并补齐 Backspace。仅改 HUD 文案和对应测试，不增加依赖。

### 第六轮新发现与修复计划

本轮原生7块/41%，三步教程确已完成；F2独立场景截图与后来CUA新画面一致。新发现：教程退出后全楼蓝图被顶部裁切；快速拖动同帧送达，ButtonInput只留下最终光标，world_click错误为true（该次落点为空，未新增块，不代表无误放风险）。先用系统回归覆盖 active→inactive 的一次性全景与单帧完整拖动，再修复；不弱化正常点击，不添加依赖。辅助代理受429限制未开始工作，HUD回归及修正由主执行完成。

### 第六轮实现和原生证据补记

- 有序 WindowEvent 回归覆盖同帧 press/move/release；原生 120×20 拖动 `world_click=false`、`orbit=true`、积木数量不变；UI 起点拖动的相机 motion 为 0。输入源保留 UI 起点所有权，复用已有节点拾取几何。
- 教程完成已原生达到 7 块 / 41%；active→inactive 一次性恢复完整楼体取景，随后保留玩家镜头操作。此前“仍裁切/拖动被当点击”不是当前已知状态，已由回归与新原生画面验证修复。
- F1 帮助以可显示的 Cmd/Shift 名称替换缺字符号，并保留 Backspace；此前原生扩展帮助可读。
- 本轮仍未通过：Cmd+Z / Cmd+Shift+Z 实际修饰键投递，存档标签/输入焦点，完整尺寸/Retina 矩阵，真实触控板手感，完整塔楼最终验收。没有把工具投递失败当作游戏快捷键已通过。
- 用户随后将接近 300% CPU 和游戏视觉质量设为最高优先级；后续记录见 `macos-performance.md` 与 `visual-direction.md`。历史推送状态以用户“已推送”为准，不改写历史。
