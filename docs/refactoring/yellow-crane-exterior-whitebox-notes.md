# 外观白模：来源、估计与未通过项

> 2026-09-08：以下保留历史研究/验收记录；最新可接手状态见[跨电脑接手说明](../handoff/README.md)。MCP 已恢复连接，但 v09 尚未完成实建/视觉验收。历史“未提交”与权限阻塞不代表当前状态。

2026-09-07。基线 `26a907c`。这是 M1 外观研究，不是实测复刻或游戏成品。

## 原图核对

### 顶冠：图 2-2-8

原图 8938 × 8029，像素原点左上。轴 3–10 的印刷尺寸为 18 m，**不是屋檐总宽**。
手工轴点 x = 2027, 2503, 2973, 3446, 4864, 5337, 5815, 6288；中心约 (4150,3818)，约 236.7 px/m。
以下都是像素换算的外观估计（原图拾取约 ±30–60 px），不是印刷尺寸：

- 主体方形攒尖屋面，四条对角屋脊汇入中央宝顶座；不是八角主屋顶。
- 主角尖约 (±12.3, ±12.1) m；每一方向另有一组副屋面，共四组、八个副角尖。
- 图上方向“北”副屋面角尖约 (±6.3,12.1) m，横脊约 x=±3.3、y=7.4 m。
- 北副屋面内回折约 (-5.9,8.2),(-4.7,7.0),(-3.0,5.9),(2.9,5.9),(4.6,6.9),(5.9,8.2) m。
- 另外三向须保留独立副屋面；东南施工详图区没有完整瓦面，镜像部分只能标记估计。
- 当前白模把副屋面回折简化成四边曲面，**尚未按上述内回折裁剪交界**。

### 下方阶梯檐口：图 2-2-4

原图 10200 × 7514，中心约 (5080,3200)，约 235.5 px/m。
东南角控制点（m）：

```
(14,0), (14,-5), (14.6,-8.6), (12.1,-8), (11.9,-8.3),
(12.6,-12.5), (8.5,-11.8), (8.1,-12.1), (8.5,-14.5),
(5,-13.9), (0,-13.9)
```

每角有三次独立翘出，不能拿顶冠近方形轮廓代替。当前中间层/底层采用这条轮廓的**临时缩放**，仅用于全楼外观研究；各层必须再分别核对对应平面，不代表这一步已完成。

## 高度与冲突

- 楼层标高继续引用已提交平面数据 0 / 6 / 12.3 / 19.4 / 26 / 32.6 m；外观只显示五个主层，内部夹层研究保留在原场景。
- 屋面内缘使用剖面候选约束：10.21 / 19.4 / 26 / 32.6 / 40.6 m；顶冠脊线 46.2 m、侧翼横脊 43.2 m。
- 檐口中心暂用 7.23 / 17.22 / 23.82 / 30.42 / 37.02 / 39.22 m。**详图曲线与具体屋面位置的映射未全部验证**。
- 四层 32.000 与 30.420、二层 17.200 与 17.220 的既有冲突保留在 eave-traces 数据，未覆盖源值。
- 第一轮把详图端点的约 1.65 m 起翘量直接用于角尖，立面对照明显偏低。第三轮独立使用约 3.5 m 的**立面轮廓估计起翘量**，不是将详图改成 3.5 m。
- 立面图 2-2-7 的 readable 文件（1614×2200）归一化到界面预览尺寸（1344×1832）后临时配准：中心 x≈675、地坪 y≈1695、约32.17px/m；来自底层30m轴跨和剖面高度的人工比对，未经去畸变。只能用于检查明显比例差异。
- 总高 49.65 m、宝顶各圈半径、墙厚、梁柱断面、栏杆和门窗分格都是占位估计，不能作为实测尺寸使用。

## 第 4 版模型及验证

独立场景 `HHL_Exterior_Whitebox_04`，本地文件 `.omx/references/yellow-crane/exterior-whitebox-04.blend`。
原有结构/分解/参考场景不删除；新建版本遇到文件或场景同名会拒绝覆盖。

- 32 个 mesh 对象，93,816 个三角面（研究模型，不是性能预算）。
- Blender 实际检查：逐对象非流形边与不一致绕向边均为 0；1,866 个连通壳体逐个检查带符号体积为正。
- 24 个纯 Python 测试通过：包含既有结构/檐口测试以及新增曲面封闭性、法向体积、无效输入、版本文件保护、单面反向/混合反向壳体、阶梯连续性、注入保存失败后的重试测试。
- 正面及斜视各渲染四轮。检查不等于精确复刻验收。
- `export_to_game=false` 是研究标记，**不是强制导出拦截器**；本轮未调用导出或改动游戏运行时。

## 尚未通过，不能进入 M2 复制细化

1. 顶冠主/副屋面在第7版改为共用接缝的连续壳体（见后文）；与下层顶檐、脊帽的接触仍未验收，角部曲率和檐口轮廓仍不自然。
2. 第 5 版已恢复底层过渡带的矩形分格体量，但分格位置、构造性质和与屋檐交界的准确性仍未验收。
3. 各层屋面仍需独立平面核对，不能把同一层的缩放估计冻结为正确建筑形制。
4. 斗拱/檐口厚度与支承、山花/牌楼面板的主要外观体量尚缺；细部纹饰可留到 M2，但支承轮廓须先补。
5. 单对象封闭性不检查跨对象穿插、自交、可行走通路、材质 UV 或游戏性能。上述各项都未验收。

下一步先处理 1–4，再冻结 M1。原始图纸、本地 Blend 文件和含原图的叠加图不进入提交或游戏分发。

## 代码复核修正

- 阶梯不再与基座横向相隔 0.29 m：从平台边缘推导位置，末阶与台面同高。
- 报告只在 Blend 保存成功后原子发布。保存异常后保留当前新场景，若没有输出文件可用同版本重试保存，不重建几何；如果已有部分文件，拒绝覆盖并要求新版本。几何验证失败保留现场供检查，使用新版本修正。
- 测试增加有向边成对检查和每个连通壳体体积，防止大正体积掩盖小反向壳体。
- Blender 检查前后十个原场景的对象名、类型、位置/缩放、顶点/多边形数量一致；这不是所有数据字段的字节级校验。


## 第 5 版：底层过渡带（2026-09-07）

本轮开始先提交第 4 版脚本/测试/计划：`3df5622`，作者与提交者均为 `ttbb <apples398@163.com>`，未 push。

### 来源与有限修正

重新检查图 2-2-7 原图局部（原图归一化范围 x=22%–78%、y=70%–81%），可见栏杆下方的窄矩形分格带。立面线稿不能确定它是玻璃窗、装饰面板或组合构造，因此新模型使用**不透明内凹面片**，不赋予透射玻璃，也不把它当作已核实的窗构造。

- 删除原地面层 4.35–12.3 m 高墙中的过高部分，围护顶部改为现有估计柱头 7.1 m；其他楼层围护高度保持不变。
- 复用二层 L2 折线轮廓，向内偏移 0.12 m；四向重复为外观研究假设，不代表四个立面均已独立核对。
- 底框 10.50–10.65 m、分格 10.65–11.45 m、上沿 11.45–12.15 m，与现有二层楼板下缘衔接。
- 分格按约 4 m 的支段和约 0.85 m 的小格自动划分，构件断面及以上高度全是**立面估计**，不是新发现的印刷尺寸。
- 继续使用批量箱体，不增加依赖；新增 5 个批量网格，不为每个小格创建独立对象。

### 验证与证据

- 独立场景 `HHL_Exterior_Whitebox_05`；本地文件 `.omx/references/yellow-crane/exterior-whitebox-05.blend`。
- 37 个网格、97,512 个三角面、2,174 个连通壳体；逐网格非流形边和不一致绕向边均为 0，每个连通壳体带符号体积为正。
- 29 项纯 Python 测试通过，新增分格带边界/尺寸、四向对称、绕向独立性、标高平移、内凹面片、错误轮廓及上层围护高度回归；先记录缺失函数失败，再实现并通过。语法编译和 `git diff --check` 通过。
- 新建场景前后原有 11 个场景的对象名、类型、世界矩阵、网格顶点和多边形索引哈希一致；未验证所有 Blender 数据字段。
- v04/v05 的 31 个既有网格顶点/面索引逐个一致。共享围护网格只有地面层高度变化，上层 832 个顶点一致；比较排除 12.3 m 边界时使用 0.001 m 容差，避免 float32 的 12.30000019 被误分到上层。
- 同相机正面、斜视及 `exterior-whitebox-05-band-comparison.jpg` 已检查。对比图沿用此前临时轴线配准，不重新拉伸模型来贴合原图。

### 验收结论

**局部修正已实现，M1 整体验收仍为 revise。** 分格带已替代整块高墙，但框架/支段位置与原图尚不逐一一致；顶部和底部仅在估计标高上衔接。顶冠交叠、各层独立平面和支承体量仍待处理；围护高度变更不是可行走室内或结构连通性验收。

本轮未修改屋面、未调用游戏导出、未改游戏运行时；未测 CPU/GPU 性能或碰撞。原图、含原图对比板和 `.blend` 留在本地，不纳入提交。下一步处理顶冠主/副屋面交界，之后再做完整立面验收。


## 2026-09-07：第6–7版，顶冠连续壳体

基线提交 `1fc264f`，本轮只替换顶冠五片独立屋面。第6版先修复投影区域重叠，第7版修复渲染中发现的副屋面正面插值凹痕。**M1仍为 revise，不是整楼真实复刻验收通过。**

### 几何方案与估计边界

- 新增纯 Python `crown_whitebox.py`，按图2-2-8的回折交界建立互不重叠的平面区域；主面和四向副面共用顶点，厚度侧壁只生成在外缘和宝顶开口，删除内部重复侧壁。不是布尔合并，也不是只调整法线隐藏交叠。
- 原主面 X 半宽12.3 m改为与Y一致的12.1 m，以四向对称建立研究面；副面内缘采用 `(5.9,8.2)、(4.7,7.0)、(3.0,5.9)` 的手工映射控制点并镜像。**这一对称化和点位都是估计，不能替代完整屋顶平面实测。**
- 保留46.2/43.2 m脊高候选、39.22/39.3 m檐高候选与0.18 m展示厚度；曲线、3.5 m角部起翘及全部插值仍是外观估计。
- 第7版副面正面改为单一梯形参数曲面，保持四条公共边界曲线，消除依赖三角剖分对角线的中央凹痕。主面/侧面仍有可见折痕；不以平滑着色宣称建筑曲率正确。
- Blender面属性 `crown_region` 保留主面0、四副面1–4，便于后续定位；尚无UV、瓦作、脊饰和游戏导出。

### 本轮已验证

- Blender MCP实际保存独立场景 `HHL_Exterior_Whitebox_06`、`HHL_Exterior_Whitebox_07`。最新文件 `.omx/references/yellow-crane/exterior-whitebox-07.blend`，33个网格、106,008个三角面、2,170个连通壳体。
- 所有网格非流形边0、绕向不一致边0，各连通壳体体积为正；新顶冠仅1个连通壳体，13,696个三角面。其3,424个顶面顶点具有不同XY坐标，13,504个面的区域属性完整。
- 35个纯Python测试通过。新增测试覆盖粗级投影三角形两两交面积为0、总覆盖面积582.4平方米（外方形减宝顶孔）、闭合/绕向/连通性、细分接缝、四向对称、脊高、参数拒绝及副面曲面回归。新凹痕回归先失败，再实现修复后通过。
- 第7版生成前的13个既有场景对象名称/类型/变换及网格顶点/面索引摘要一致。此证明不涵盖全部Blender数据块。与第5版逐项比较，32个非顶冠网格完全不变，原5个顶冠网格替换为1个。
- 已检查正面/斜视、05/06/07同相机顶冠对比及2-2-7局部立面对照；原图沿用临时配准，未为贴合本版重新调整比例。
- 证据均位于本地参考目录：`exterior-whitebox-07-{validation,preservation,scope-regression,crown-audit}.json`、`exterior-whitebox-07-{front,oblique}.png`、`exterior-whitebox-07-crown-comparison.jpg`、`exterior-whitebox-07-source-crown.jpg`。

### 尚未通过 / 下一步

1. 顶冠正面中部檐线仍过于弯曲；左右坡面褶皱、尖部曲率和下层顶檐连接不符合精确立面要求。下一步按立面控制点约束檐线，再处理下层顶檐衔接，而非直接进入贴图。
2. 脊帽接触、全楼支承轮廓/门窗轴位、逐层独立屋顶平面、宝顶仍未完成。
3. 面数比第5版增加8,496（约8.7%），这是拓扑研究而非性能优化。未导出游戏、未运行游戏回归或Mac CPU/GPU测试，不能推断运行时表现。
4. 独立代码审查子代理因上游错误未完成；已做本地差异审查、单元测试、Python编译检查和Blender实建验证，不记为独立审查通过。

原图、含原图对照、`.blend` 和本地状态不纳入Git。旧版本保留，M2–M4仍等待M1通过。


## 2026-09-08：第8版，顶冠中部檐线约束

第6–7版已提交为 `d465a04`，作者与提交者均为 `ttbb <apples398@163.com>`，未push。本轮仅修改顶冠四副面正面及公共檐线，保留旧场景、所有平面分区和其他网格。

### 修正与来源边界

2-2-7局部立面对照显示中部檐口为近水平直线，第7版将角部起翘分布到整条檐线，形成碗形轮廓。第8版使用同一个纯函数计算正面及公共檐边：中段不增加角部抬高，两侧肩点之外再逐渐起翘，脊线/侧斜脊边界和檐口端点保持原值。

肩点名义半宽3.3 m复用原临时半脊宽，**并非新测得的檐口尺寸**；默认网格离散后的水平顶边延伸至约±3.15 m，不能把解析曲线的名义宽度当作有限网格精度。39.3 m檐高、42.8 m尖点及曲率仍是外观候选。新方案复用已有梯形参数面，没有增加细分或平滑修改器。

### 检查结果

- 先新增中段平直/两侧单调起翘测试，在第7版代码上失败；实现后36个Python测试通过，Python编译检查、`git diff --check`通过。
- Blender MCP实际建立并保存 `.omx/references/yellow-crane/exterior-whitebox-08.blend`，33个网格、106,008个三角面，面数不变；非流形边与绕向不一致边均为0，各壳体体积为正。
- 14个旧场景的对象名称/类型/变换、网格顶点和面索引摘要不变（不等于全部数据块的保存证明）。与第7版相比，32个非顶冠网格完全相同；顶冠XY、面索引和区域属性完全相同，仅高度变化。
- 固定相机正面/斜视、2-2-7临时配准对照已检查，中段轮廓改善。证据：`exterior-whitebox-08-source-crown.jpg`、`exterior-whitebox-08-crown-comparison.jpg`、正面/斜视PNG，以及 `exterior-whitebox-08-{validation,preservation,scope-regression}.json`。
- 使用Blender网格三角面，在0.5 m XY网格上取顶冠最低表面与下层顶檐最高表面的垂直间距。1,540个共同投影采样点未发现负间距，最小约0.639 m，最大约4.404 m；见 `exterior-whitebox-08-contact-samples.json`。这是**有限采样诊断**，不排除采样间穿插、不证明全局最小距离，更不证明层间支承/建筑构造准确；不能因为存在间距就把两层屋面焊接填满。
- 本轮独立审查子代理再次因上游错误未运行完成。已做本地差异审查、测试和实建；未宣称独立审查/LSP通过。

### 保留风险与下一步

**局部檐线修正完成，M1整体验收仍为revise。** 尖部曲率仍偏硬，主副面侧坡折痕、下层顶檐轮廓、支承与封檐体量缺失没有解决。下一步优先对照剖面/立面补齐顶层支承轮廓、核对侧坡及层间关系，不先做瓦片贴图掩盖形体差异。

此次未修改游戏运行时、未导出、未测CPU/GPU；保持M2–M4等待状态。原图及本地Blend/对照资料继续不纳入Git。

## 2026-09-08：顶层支承预检，Blender检查尚未完成

第8版已提交 `245159c`，作者/提交者均为 `ttbb <apples398@163.com>`，未push。本轮核对2-2-5剖面后新增纯几何支承预检，不把缺少尺寸的分层支承称为真实斗拱。

### 已完成的代码与发现

- `top_support_whitebox.py` 按已有L5外围柱轴选取20处候选，复用原盒体批处理。四级宽度0.48/0.64/0.84/1.04 m均为研究估计，保留原37.75 m柱顶，不将其混同于37.3 m候选标高。
- 将向下三角面裁剪到整个支承方形范围，以裁剪顶点最低Z限制平顶，留0.02 m间隙；不再仅检测柱中心。输入约定为XY投影不重叠的单层屋面下表面，面积覆盖检查不适用于任意多层重叠网格。
- 预检发现四角柱(±9,±9)的1.04 m宽支承与现有屋面不能兼容。纯Python扇形三角化诊断：中心点下表面高于38 m，但整个范围的最低点约37.765 m，距原柱顶仅约0.015 m。这个数值**不是Blender实际三角化的已验收结果，也不是测得的建筑尺寸**。
- 现在保留冲突列表，不自动缩窄支承、移动柱或修改屋顶来隐藏问题。纯几何结果为16处可生成、4处阻断，64个盒体，候选增量768三角面。输出全部标记为估计代理，不能声称承重连接成立。

### 验证与阻塞

新增测试先失败，实施后46个Python测试通过；Python编译、`git diff --check`通过。测试覆盖全范围最低点、内部极小值、缺失覆盖、朝向、非法参数、分级接触、外围柱筛选及四角冲突保留。纯预检证据为本地 `top-support-preflight-20260908.json`。

实际调用Blender MCP尝试建立09时，旧的严格保护以 `Insufficient space for support proxy` 中止；**没有成功保存或渲染第9版**，中间场景不删除。随后一次诊断及一次重试均返回自动权限审核超时。修正后的冲突报告代码尚未在Blender重跑，不能宣称新模型验收、旧场景摘要验证或视觉改进通过。

最新完整白模仍是08；本轮新增代码尚未提交。待工具执行授权恢复后重载 `top_support_whitebox`，使用新版本10，保留失败的09场景，做实际三角化、旧场景/33个原网格不变、渲染及视觉检查。四角屋面/柱头关系必须另行按图约束，不通过放宽保护阈值掩盖。M1仍revise，M2–M4未开始，游戏代码、导出和CPU/GPU验收未改变。


## 2026-09-08 — 只读检查后恢复建模（v10，当前状态）

此前权限超时与“最新08”是历史状态。本次只读检查成功后，经 Blender MCP 使用现有代码
建立并保存10，保留未完成09。34个网格、106,776个三角面；旧16个场景的对象/变换/网格
摘要不变，08的33个网格顶点、面索引、变换和顶冠区域属性完全不变。检查不覆盖所有数据字段。

新增一个支承网格，含16处、64个封闭方块；四角冲突保持显式阻断。实际Blender三角化后的
全方形覆盖范围检查，最小垂直净空约0.020 m（浮点0.01999785 m），层间和柱顶连接误差在
1e-5 m内。拓扑检查非流形/绕向不一致边为0，各连通壳体体积为正；不证明全局无自交或结构准确。

正面、斜视和檐下近景已渲染并检查，结合图2-2-5剖面参考，M1仍revise：支承只是方块占位，
转角柱头关系未解决，屋檐尖部及侧坡折痕仍偏硬。不以“能生成”代替真实复刻验收。
下一版11先核对转角柱头/下层顶檐关系，不缩小构件或放松保护掩盖冲突。

证据见交接目录的 `exterior-whitebox-10-{validation,preservation,scope-regression,support-fit,visual-verdict}.json`。
原始10多场景文件及三张PNG只在本地忽略目录；仓库可移植白模仍08。
M2–M4未开始，未修改游戏代码、导出资产或进行CPU/GPU性能验收。


## 2026-09-08 — 四角最低点诊断（研究11，不是外观白模11）

v10证据已提交为 `2477de7`。本轮先写计划并新增三个失败的最低点见证测试，再复用原裁剪
逻辑返回最低点与三角形索引；原标量API、宽度、净空、最小高度以及全部原有测试保持不变。
没有以临时标高替换柱高。图2-2-5的37.300 m是暗层标高，不是已证实的外柱柱顶。

MCP独立场景 `HHL_Corner_Clearance_Study_11` 已保存和渲染。实际屋面三角化下，四角
全范围最低37.76517667 m，柱顶37.750 m；相对占位规则要求 >37.810 m，差44.823 mm。
最低点距柱心约0.735 m，位于支承方形角点；中心1 mm探测约38.3213 m，会误判净空充足。
因此当前问题定位到角柱支承覆盖范围遇到下层顶檐下降坡段，尚未证明屋面或柱高哪个应调整。

渲染后旧17场景对象变换/网格/顶冠区域摘要及v10原文件哈希不变。四个见证点重心坐标均
在对应三角形内，平面残差为0；同版本重复构建在修改前拒绝。49项建模测试、4项交接测试
通过；Python语法检查及差异格式检查通过。独立审查子代理因上游错误未完成，不宣称审查通过。

诊断渲染可读性通过，不提升整楼M1的revise结论。采样剖线为41个1 mm方形探测、非等比例
绘制；最低点来自全方形裁剪，不依赖剖线采样。未复读诊断blend，未完成外观、结构或游戏验收。
数值证据见交接目录 `corner-clearance-study-11{,-verification}.json`；原blend/png不进Git。
下一外观迭代12必须先取得/确认四角构造约束，不按44.8 mm差额直接抬顶降柱。


## 2026-09-08 — v12：修正五层轮廓的额外缩放

研究11已提交为`64a6708`。核对2-2-4五层平面与2-2-13翼角详图后发现，既有轮廓已取自五层，
但五层生成又乘0.96。回归测试在旧比例下六点全部失败，改为1.0后50项建模测试通过。六点
误差由18.97–27.51 px降至0.43–5.50 px（6 px是人工取点/轮廓取整容差，不是测绘精度）。
这是取消无来源的额外缩放，不是按44.8 mm净空差调几何；其他层、柱高、标高、屋面曲率、
支承宽度/高度下限/间隙规则均不变。来源与局限见`yellow-crane-l5-eave-landmarks.json`。

MCP实建保存外观12：34网格、106,968三角面；顶层支承20处可生成、0处阻塞，四角支承高
约0.21737 m，柱顶仍37.750 m。实际顶面全覆盖最小垂直间隙0.01999806 m（3e-6 m浮点容差）。
原18场景几何摘要及v10原文件不变；仅五层下檐和支承两网格变化，32网格不变。拓扑检查
非流形/绕向错误0，各独立壳体正体积。不证明全局无交叉、真实斗拱或承重关系。

正面、斜视、两张近景和本地平面叠图已检查；M1仍revise，屋面折痕/尖翘和构件形态未通过。
独立代码审查未发现可操作问题，LSP/AST-grep不可用，没有正式静态审批结论。未复读v12 blend，
未修改游戏或做运行时性能验收。数值证据在交接目录；原图/叠图/原始blend不入Git，可移植仍08。
下一版13优先结合平面斜长与真高修正屋面/翼角曲率，不把本次六点与占位净空通过当作整楼验收。


## 2026-09-08：v13 五层檐尖标高约束（已保存，验收未完）

v12提交为`ca75008`。图2-2-14对称翼角标注五层尖端38.800、低檐37.020 m；旧统一起翘3.5 m使
五层尖端达到40.520 m。本次只将五层起翘差改为1.780 m；平面1.0、内环40.6、厚度.18和
原插值/细分不变，其他楼层不改。十二尖的镜像对应仍是暂定，不能把端点约束说成完成实长展开。
没有使用虚线檩条示意曲线，也未借用仅适用于二至四层的非对称描点。四层来源冲突保留。

回归测试先以40.52失败，再以38.8通过；总51项通过。Blender MCP构建13成功，34网格/106,968三角面，
构建拓扑检查通过；20支承可生成。原19场景几何摘要及v12文件保持不变。之后权限审批连续超时，
未取得实际网格读回、独立净空复验、32网格范围对比和渲染结果。代码审查COMMENT无可操作发现，
但指出纯函数测试不覆盖生产分支，需补实际场景端点读回；LSP不可用。

v13不评分、不导出，M1沿用最近v12视觉结论revise。恢复MCP后先验收现存13，不重新覆盖，
数值证据见交接清单；本机原始多场景文件不纳入Git。完整翼角曲线/连接仍待处理。


## 2026-09-08：v13 复验补完（建模提交02fb029）

MCP恢复，在已有13上完成12檐尖端点、32未动网格比较与20支承实际顶面足迹检查。
最小竖向间隙0.0199977854 m（允许3e-6 m浮点误差），最小支承高0.194633484 m；
支承裁剪算法与生成器共用，不宣称独立算法证明。保存文件通过同哈希临时副本scene-library复读，
36对象/34网格的几何与变换一致；加载后需先更新视图层，避免相机未求值矩阵造成假差异。
没有覆盖保存文件，全部20场景几何摘要不变，临时加载数据和相机已清理，重复版本保护通过。

正面、斜视、檐下近景对照本地2-2-7参考已检查，M1仍82/revise，不因本轮几何检查通过而晋级。
下一步应建局部翼角平面斜长—展开真高对应，处理曲线/连接，再复查支承足迹；不是增加细分或贴材质。
51项建模、5项交接测试通过；本轮未改建模生产代码，可移植资产仍是08。


## 2026-09-08：v14 有界一/三层端点修正（前置复验提交463099d）

图2-2-14注明高度为真高、长度应按平面斜长展开，虚线不作放样用；完整曲线映射仍缺证据。
因此先分别采用同图一层9.010/7.230、三层25.600/23.820的对称标注，替换统一3.5 m估计起翘。
两层尖端各下降1.720 m；中间Z仍是原估计函数，不是完整曲线复刻。二/四层冲突不动，五层约束保留。
生产入口改为共用`canopy_roof_mesh`，消除循环里的五层特判，按独立来源配置端点；无新依赖。

两项回归先失败再修复，53项纯Python测试通过。MCP另存14，34网格/106,968三角面不变，
实际保存网格120端点读回通过；32未动网格、20旧场景和v13文件保留。后台读取保存文件时须同等
求值两份旧场景后比较，避免旧缓存矩阵假差异。后续短MCP比对确认磁盘/当前v14几何一致。
五层20完整支承足迹仍满足0.020 m减3e-6 m容差。下层柱包围方形68处完整覆盖且无新增竖向交叉，
48处不完整覆盖明确未检查，不能当通过。裁剪复用生成助手，非独立算法或全场景结构验收。

正/斜/檐下三视已检查，M1维持82/revise：二四层过翘、屋面折痕、顶冠鼓包、连接暗缝及构件层次待改。
独立代码审查无可操作发现，但LSP不可用。原始文件/图片不提交，可移植仍08；后续改形使用新版本15。

交接回归进一步锁定：68处已检查方形中，三层16处v13已是负间隙，v14部分最多加深
0.00300385 m，最低约-0.229167 m；“没有新增负值”绝非净空验收。需核真实柱截面而非只看
保守方形，且不能为隐藏风险降低柱高。6项离线交接测试锁定该限制，下一步优先诊断。


## 2026-09-08：柱截面源码预检，未完成实际网格复读

请求的v14提交自动审批两次超时，未执行，HEAD仍463099d。MCP初始场景查询正常，但后续执行审批
及沙箱外后台只读启动审批均连续超时；沙箱内后台启动退出139。没有更改模型或保存新版本。
新增独立诊断助手，以柱顶真实多边形形式做裁剪，并严格核验直棱柱连接拓扑。审查发现重复半覆盖
可掩盖缺口、畸形侧面可冒充柱体，补回归先失败再修复。生产支承方形算法没有改变。

生成源码预检的116柱中68完整、8部分、40无覆盖；完整者仍16处三层负间隙，最低-0.227263 m，
最大加深约4.808 mm。但该数值来自双精度源码顶点/扇形三角化，不能替代上轮Blender实测方形数据，
不能据此确认真实实体穿模，也没有进行独立实体相交验证。本轮不做新视觉评分，沿用M1 revise。
