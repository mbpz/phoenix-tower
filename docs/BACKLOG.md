# 开发任务清单（Backlog）

> 从 docs/PRD.md 拆解出的可独立领取任务（垂直切片，tracer-bullet 风格）。
> 类型：**AFK** = 无需人工可独立实现；**HITL** = 需要人工介入（设计/美术/决策）。
> 依赖关系以 `← B-xx` 标注。本地仓库暂未接入 Issue 追踪器，先以本文为准；接入后按依赖顺序发布为 issue 并打 `needs-triage` 标签。
> 评审修订项（docs/REVIEW.md）已并入对应任务。

---

## Phase 0 —— 技术验证（2-3 周）

| ID | 类型 | 依赖 | 任务 | 验收标准 |
|----|------|------|------|----------|
| B-01 | AFK | — | **Bevy 0.19 工程骨架 + 空场景**（已交付） | `cargo run` 出窗口；场景含地面与光照；CI 上 `cargo check` 通过 |
| B-02 | AFK | B-01 | **轨道相机**（已交付） | 左键旋转 / 滚轮缩放 / 右键平移，参数有界不翻转 |
| B-03 | AFK | B-01, B-02 | **射线放置单积木**（已交付） | 点击在地面网格放置 1×1 立方体；幽灵预览实时吸附；重叠禁止；Backspace 撤销最后一次 |
| B-04 | HITL | B-03 | **技术验证验收** | 独显档 ≥ 60 FPS、放置延迟 < 50ms、无 panic 连续运行 30 分钟；产出验证报告 |

---

## Phase 1 —— MVP（6-8 周）

| ID | 类型 | 依赖 | 任务 | 验收标准 |
|----|------|------|------|----------|
| B-05 | HITL | — | **黄鹤楼简化蓝图资料包**：三视图 + 部件清单 + 1:100 比例网格图 | 产出 30-40 种积木的尺寸/位置/层级定义，可导入 RON |
| B-06 | AFK | B-03 | **积木定义数据驱动化**：`resources/blocks/*.ron` + serde 加载；含 ID/尺寸/材质/层级/文化描述（✅ 已交付 2026-08：15 种积木、多尺寸放置、O(1) 占用查重；⚠️ 热重载留待 dev 特性） | 新增积木仅改 RON 不动代码；热重载（dev 特性）生效 |
| B-07 | AFK | B-06 | **积木库与资产**：30-40 种积木的低模 glTF（≤500 三角面）+ 朱红/黄琉璃/青石/木纹材质（⏳ 进行中：15/30-40 种已定义，占位立方体；glTF 模型待美术） | 积木面板可选任意积木并放置；占位模型可运行 |
| B-08 | AFK | B-05, B-06 | **蓝图模式**：半透明幽灵蓝图 + 严格吸附 + 稀疏格子匹配（✅ 已交付 2026-08：M 键切换、81 格占位黄鹤楼蓝图、红/绿幽灵反馈、完成度实时计算；匹配算法单测 6 例通过；⚠️ 旋转匹配与已放置错误高亮留待 Phase 1 完整版） | 放置正确格高亮确认、错误格红色反馈、缺失格幽灵提示；匹配算法单测通过（对齐/偏移/旋转/多余四情形） |
| B-09 | AFK | B-08 | **完成度计算**：层级加权公式（✅ 已交付 2026-08：ADR-005 加权公式 + 结构正确率 + ≥95% 完成事件，单测覆盖；⚠️ 进度条可视化归入 B-10 UI） | ≥95% 触发完成事件；进度条实时更新 |
| B-10 | AFK | B-06 | **基础 UI**：bevy_egui 积木面板 / 撤销重做按钮（≥20 步 Command 历史，替代 B-03 的简单栈）/ 蓝图透明度滑杆 / 进度条；**打包 CJK 字体**（✅ 2026-08：Noto Sans CJK SC 子集化 289KB 已入库并用于 HUD，全中文可渲染；⏳ egui 面板/进度条/撤销重做待做） | 全中文 UI 无缺字；撤销重做 20 步可用 |
| B-11 | AFK | B-08 | **本地存档**：`.ptw`（bincode）+ JSON 导出；版本号 + 迁移链（✅ 已交付 2026-08：F5 保存 / F6 导出 JSON / F9 读取；原子写入；版本迁移链；单测 3 例：bincode 往返一致/当前版本接受/未来版本拒绝） | 存/读往返一致；旧版本号迁移正确；中断写入不损坏 |
| B-12 | AFK | B-08, B-10 | **新手教程**：前三步强制蓝图（基座→柱→一层屋顶），教学吸附/旋转/删除 | 教程完成率埋点；可跳过不阻塞自由模式 |
| B-13 | AFK | B-03 | **场景基础**：蛇山地形 + 长江远景 + 昼夜切换（✅ 已交付 2026-08：蛇山三级台地、长江水面、T 键昼夜平滑过渡；⏳ 正式地形高度图/雾/天气 Phase 2） | 相机任意视角无穿帮；昼夜切换 1s 过渡 |
| B-14 | HITL | B-10 | **MVP 玩法验收**：蓝图模式全流程可玩、存档可靠、无致命 bug | 内部 5 人试玩清单全部通过 |

---

## Phase 2 —— 完整版（8-12 周）

| ID | 类型 | 依赖 | 任务 | 验收标准 |
|----|------|------|------|----------|
| B-15 | AFK | B-11 | **自由创造模式**：无蓝图、多楼层扩展、无失败状态 | 可搭建 ≥ 5000 积木不卡顿；支持保存/分享 |
| B-16 | AFK | B-11 | **挑战模式**：限时/限料/限旋转 + 星级评价（REVIEW P2：风向为装饰性规则） | 三种规则可组合；星级与达成度挂钩；稀有积木奖励入库 |
| B-17 | AFK | B-15, B-16 | **avian3d 集成 + 结构稳定性挑战变体**（ADR-002；REVIEW P4） | 拆除支撑积木后上部坍塌；稳定性评分可复现（种子化） |
| B-18 | AFK | B-16 | **图鉴与成就**：完成蓝图解锁部件图鉴 + 文化小知识；成就：首次完成/限时通关/无错误/大体积 | 图鉴条目与 RON 文化描述联动；成就触发埋点 |
| B-19 | AFK | B-15 | **音频**：放置碰撞声 / 完成编钟 / 长江水声 + 风铃环境音 | 音效随操作即时播放；音量分级可调 |
| B-20 | AFK | B-15 | **性能优化**：分块 + 脏标记 + 异步贪婪网格化 + 实例化渲染 | 独显档 50000 积木 ≥ 60 FPS；集显档 5000 积木 ≥ 30 FPS |
| B-21 | AFK | B-15 | **截图 / 短视频导出**（内置录制） | 一键截图 PNG；录制 ≤ 30s 导出 MP4 |
| B-22 | HITL | B-20, B-21 | **完整版验收**：全模式可玩、性能达标、压测 1 小时无泄漏 | 验收报告 + 性能基线存档 |

---

## Phase 3 —— 运营与扩展

| ID | 类型 | 依赖 | 任务 | 验收标准 |
|----|------|------|------|----------|
| B-23 | HITL | B-21 | **分享机制**：本地文件分享（`.ptw`）先行，云端分享码后置（REVIEW P6） | 导入/导出他人存档成功 |
| B-24 | AFK | B-18 | **主题包框架**：滕王阁/岳阳楼蓝图包（复用全部系统，仅换 RON 蓝图与贴图） | 换包不改代码；包格式文档化 |
| B-25 | AFK | B-10 | **多语言 + 无障碍**：吸附高亮不依赖纯颜色（叠加轮廓/闪烁）、全键鼠可玩、结构提示朗读 | 中/英切换；色盲模拟下提示仍可辨 |
| B-26 | HITL | B-20 | **Web（WebGPU）/ 移动端评估**：可行性 + 降级方案 | 产出评估报告与决策 |

---

## 指标埋点（REVIEW §三）

| ID | 类型 | 依赖 | 任务 | 验收标准 |
|----|------|------|------|----------|
| B-27 | AFK | B-12, B-14 | **成功指标埋点与仪表盘**：教程完成率 / 蓝图完成率 / 会话时长（新老玩家分档）/ 作品保存率 / 分享率 | 埋点随 Phase 1 上线；周报自动汇总 |

---

## 依赖总览

```
B-01 → B-02 → B-03 → B-04 (Phase 0)
                ├── B-06 → B-07 → B-08 → B-09 → B-10 → B-12 → B-14
B-05 ───────────┘      └───────→ B-11 → B-15 → B-16 → B-17 → B-18 → B-22
                                              └──→ B-19 → B-20 → B-21
B-13 独立并行
```
