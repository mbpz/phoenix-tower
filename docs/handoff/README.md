# 跨电脑接手说明

更新：2026-09-08。本文件是持久交接入口，不是聊天记录备份，也不会自动恢复 OMX 执行模式。
历史笔记/计划如与本页状态冲突，以本页和重新取得的 Git、文件、测试证据为准。

## 先读什么

1. [AGENTS.md](../../AGENTS.md)：提交身份与协作约束。
2. [游戏 README](../../README.md)、[交付计划](../design/yellow-crane-delivery-plan.md)。
3. [白模研究说明](../refactoring/yellow-crane-exterior-whitebox-notes.md)、
   [来源登记](../refactoring/yellow-crane-reference-register.md)。
4. [.omx 历史计划](../../.omx/plans/README.md)：需要理解具体决策时再阅读。
5. [新电脑环境配置](environment.md)；只读白模不需要先恢复私人参考图库。

## 本轮接续：Blender运行时复验已恢复，16处交接风险得到复核

- 开始时工作区干净，上一阶段已提交为 **`cccc0b1`**，作者/提交者均为
  `ttbb <apples398@163.com>`；没有重复制造空提交。本轮新增提交状态以Git实查为准，未推送。
- **Blender MCP只读查询和Python执行成功**，当前场景`HHL_Exterior_Whitebox_14`，36对象。
  读取v13/v14的8个柱/屋面网格及实际`loop_triangles`，共55,520个三角形。
  两场景更新依赖图后计算的前后几何摘要一致，活动场景及v13/v14磁盘文件哈希未变。
  未保存、修改或新建模型；先前审批超时不再是本阶段阻塞。
- [实际运行时诊断](yellow-crane/evidence/post-contact-v14-runtime.json)逐面校验三角形索引、
  数量及有向边界；8个网格的坐标/面哈希全部匹配既有存储网格提取结果，不使用替代对角线。
  v13/v14均为 **116柱：68完整覆盖、8部分覆盖、40无覆盖；三层16处负间隙**。
  最低约 **−0.227263154 m**，既有负间隙最大加深约 **0.004807344 m**。
- 两版本各16处均找到公共内部见证点：除发现阶段的柱棱柱外，另对**实际柱三角形**以及
  实际屋面三角形计算有向立体角，绕数均约1。已补齐运行时三角化证据，不再只是离线估算。
  这是有闭合/绕向检查的局部内部见证，**不是全局自相交检查、布尔交叠体积或结构真实性验收**。
- 48处部分/无覆盖仍为未验收；不降低柱高、不调整屋面来掩盖风险。
  模型仍v14、**M1仍82/revise**，可移植资产仍v08；没有新渲染、游戏资产导出或性能结论。
- 76项建模诊断测试、9项离线交接测试及Python语法/差异格式检查通过。新增实际三角化验证
  复用既有裁剪与绕数工具，无新依赖；独立复审发现的元数据覆盖计算结果问题已修复，
  新增回归并经只读复审通过。
- 下一步：按三层柱头/檐下构造参考，确定柱顶标高与屋面曲率各自的来源约束，再在新v15中
  做有依据的局部修正。继续保留v14和来源冲突，不能按22.73 cm诊断差额直接削短柱子。

### 上轮历史记录：存储网格诊断（运行时限制已由上节补齐）

- 已提交上一轮v14与源码预检：**`6017cc5`**，作者/提交者均为`ttbb <apples398@163.com>`，**未推送**。
  下方此前审批阻塞仅是历史记录。本节存储网格诊断是该提交之后的新工作，提交状态以Git实查为准。
- 本轮MCP执行与后台Blender启动各重试一次，均自动审批超时，**没有执行成功**。没有再尝试改形，
  没有新增15；当前模型仍14，不宣称本轮取得了Blender运行时三角化或新视觉验收。
- 改用沙箱内只读SDNA提取保存文件的8个目标对象：v13/v14各一、三层柱及屋面。
  文件SHA-256与既有后台Blender复读记录一致；检查本地指针作用域、数组长度、索引、恒等变换及无修改器等条件。
  全部顶点/面另与float32生成器夹具交叉核对，坐标差0、面索引一致；这是提取正确性检查，不是建筑真实性验证。
- [存储网格诊断](yellow-crane/evidence/post-contact-v14-stored-mesh.json)使用真实存储的16边柱截面，
  对每个屋面四边形分别采用两种明确对角切法。两种结果均为116柱中68处完整覆盖、8处部分覆盖、40处无覆盖；
  **三层同样16处负间隙**，最低分别约−0.227263154 / −0.227263165 m。
- 使用独立于高度裁剪的**有向立体角算法**，v13/v14各16处都找到同时位于柱体及两种重三角化屋面内部的见证点。
  检查闭合边、绕向和边界距离；不接受仅接触表面的点。证据保存坐标、两侧绕数与公共竖向区间，
  不是只把“负间隙”重命名成实体穿模。
- **仍不是实际Blender loop triangles或最终实体相交验收**。未穷尽混合切法/自相交；
  私有一次性SDNA提取脚本依赖本机Blender头解析模块和Python 3.14，未作为可移植解析器分发。
  按两种切法，既有负间隙最大加深分别约**4.807 / 5.195 mm**，不能混用上一轮包围方形的3.004 mm。
- 70项建模诊断测试、8项离线交接测试通过；独立只读复审为COMMENT、0项可操作发现，LSP不可用。
  没有生产几何改动、新依赖、导出或性能结论。
  **M1仍82/revise，可移植资产仍v08**。下一步恢复MCP只读实际loop triangles复验，再决定三层交接修正；
  不得因上述诊断直接降低估计柱高，也不得将48处部分/无覆盖计为净空通过。

### 上轮历史记录：源码预检（提交阻塞已于本轮解除）

- 用户要求先提交v14再继续。作者/提交者检查均为`ttbb <apples398@163.com>`；提交操作两次自动审批超时，
  **没有执行成功，v14和本轮接续工作均未提交、未推送**。当时HEAD仍为`463099d`，后续以Git实查为准。
- 开始时MCP只读场景查询成功，当前为14、36对象。随后MCP执行两次审批超时；后台Blender沙箱启动崩溃，
  沙箱外只读启动审批也两次超时。没有改动屋面/柱体，没有另存15，也没有恢复旧执行模式。
- 新增纯Python凸多边形检查与实际直棱柱拓扑提取器，拒绝重复覆盖掩盖缺口、重复侧面及交叉柱底。
  保留生产方形支承拟合逻辑不变，无新依赖。61项建模测试通过。
- [柱截面源码预检](yellow-crane/evidence/post-contact-v14-source-preflight.json)覆盖116柱，
  使用生成器的16边截面，而非0.48 m包围方形：68处完整覆盖中仍16处负间隙；8处部分覆盖，40处无覆盖。
  预检最低约-0.227263 m，v13→v14最大加深约0.004808 m，**不是上一轮Blender方形检查的3.004 mm**。
- **该预检使用双精度生成顶点和扇形三角化，不是已保存Blender网格的实际loop triangles**。
  不替代现场复读，不证明实体穿模；部分/无覆盖也不记为通过。参考图、柱高与直径仍有估计边界。
- 下一步审批恢复后先补提交，再读取v13/v14实际柱截面/屋面三角形，并用独立实体相交见证复核16处风险。
  不能据源码预检降低柱高。M1继续沿用v14的82/revise，本轮无新的渲染或游戏性能验收。

## 最近已复验模型：v14 一、三层端点修正，视觉仍未通过

- 上一轮v13复验已提交为 **`463099d`**，作者/提交者均为 `ttbb <apples398@163.com>`，未推送。
  本节v14是该提交之后的工作，提交状态应另查Git。
- 经Blender MCP新建并另存 **`HHL_Exterior_Whitebox_14`**，34网格、106,968三角面，未增加细分。
  一层檐尖10.730→9.010 m，三层27.320→25.600 m，均去除原估计值高出的1.720 m。
  低檐分别保留7.230/23.820 m；五层保留38.800/37.020 m及自身平面比例。
- 只改动`Ground_canopy`和`Third_canopy`，其余32网格不变。XY、面拓扑、内圈屋脊标高、
  0.18 m壳厚及全部柱高保留。中间Z仍用估计公式；**不是仅改尖点，也不是完整曲率校准**。
  二、四层来源冲突未解，保持不动不代表通过。十二尖平面对应仍暂定。
- 单测先捕获两处1.720 m偏差再修正；53项建模、6项离线交接测试通过。实际保存文件核对一/三/五层共120端点，
  含本次24个修正尖点及12个保留五层尖点，容差3e-6 m。34网格非流形/绕向错误为0，各壳体体积为正。
- 后台Blender复读与后续短MCP摘要比对共同确认保存文件/当前v14几何一致，20个旧场景保留，
  原v13/v14文件未变，重复版本保护通过。早期大批量MCP审批超时已绕过；三处旧场景缓存变换
  差异通过两个保存文件同等依赖求值后比较排除。不是连接仍阻塞，也不是旧场景损坏。
- 五层20支承全足迹竖向净空复查最小0.0199977854 m，满足0.020 m减浮点容差；复用生成器的
  已测试裁剪助手，非独立算法。一/三层68个完整覆盖的0.48 m柱包围方形未新增竖向交叉；
  **其中三层16处本来已有负间隙，部分加深最多0.00300385 m，最低仍约-0.229167 m**；
  无“新增负值”不等于净空通过。这是保守包围方形，不直接证明真实柱截面穿模，需要下一步核验。
  **另48个未被屋面完整覆盖的方形未检查**，不可将其记为通过。未做全场景交叉或结构受力证明。
- 正面、斜视、檐下近景均渲染并检查；默认斜视/1400×1600已恢复，临时相机移除。
  **M1维持82/revise**：屋面折痕、二四层尖翘、翼角连接与构件层次仍不合格，82不是吻合百分比。
  没有导入游戏，也未宣称交互/CPU-GPU改善。可移植场景仍v08。
- [v14验证汇总](yellow-crane/evidence/exterior-whitebox-14-final-verification.json)说明了有界验证和未验收项；
  readback为中间阶段记录，其中“尚未比对当前场景”的历史限制由live-match/post-verification补完。
  原始多场景文件与渲染图片仅保留本机，仓库仅收录审阅后的数值JSON。

## 上一检查点：v13 几何复验完成，视觉仍需修正

- v13建模已提交为 **`02fb029`**，作者/提交者均为 `ttbb <apples398@163.com>`；本轮未推送。
- 本轮MCP只读查询恢复，在**已有13**上完成验收，没有重建、覆盖或增加模型细分。
  34个网格、106,968个三角面；五层12个檐尖实际读回均符合38.800 m，低檐37.020 m（3e-6 m容差）。
  十二尖平面对应仍是暂定，不能等同于已完成测绘复刻或实长展开曲线。
- 相对v12仅`Fifth_lower_canopy`及`L5_support_PROXY`变化，其余32个网格几何/变换/顶冠区域属性一致。
  20处生成后支承足迹复查：最小竖向间隙0.0199977854 m，在3e-6 m浮点容差内满足0.020 m；
  最小支承高0.194633484 m，柱顶仍37.750 m，宽度仍1.04 m。复查复用了已测试裁剪助手，
  不是独立算法，不证明全场景无交叉、欧氏距离净空或真实结构受力。
- 已从v13保存文件的SHA256一致临时副本隔离加载13场景，更新依赖后与当前36对象/34网格的
  顶点、面、变换和顶冠区域摘要比较一致；加载数据已移除。原文件和全部20场景几何摘要保持不变。
  原文件不能直接自载、相机矩阵需更新两个验收脚本问题已排除，不是模型损坏。
- 正面、斜视、檐下近景均使用v12机位渲染并检查。**M1仍82/revise**：五层尖端高度偏差修正，
  但屋面折痕、翼角连接和真实构件层次不合格；82是定性评审信号，不是测绘吻合百分比。
  M2–M4、游戏导入/交互/性能验收未开始或未改变。临时相机移除，默认机位/分辨率恢复。
- 51项建模测试、5项离线交接测试通过。新增交接测试锁定已发布证据的端点、净空及“未视觉通过”边界，
  不冒充一次新的Blender运行。重复构建13被拒绝且未切换场景。
- [v13本轮复验汇总](yellow-crane/evidence/exterior-whitebox-13-final-verification.json)取代
  [原部分验证记录](yellow-crane/evidence/exterior-whitebox-13-checkpoint.json)中的运行阻塞状态，后者作为历史保留。
  原始文件与图片仅本机保存；仓库可移植场景仍为v08，本轮没有做新的可发布依赖审计或portable导出。

## 上一阶段已检查状态：v12（以下净空/渲染数据不代表v13）

- 游戏仍是可编辑庭院原型；黄鹤楼完整复刻、美术与运行时验收未完成。
- 顶层支承代码基线是 `cdfc5de`；v10 验证记录已提交为 `2477de7`。
  v08 建模提交为 `245159c`。历史文档中“本轮未提交”只描述当时，不代表当前状态。
- 上一阶段已检查外观检查点为 **v12：34个网格、106,968个三角面**。经Blender MCP实建、保存、渲染。
  五层轮廓原已来自图2-2-4，却额外乘0.96；本次删除该多余缩放，保持标高/柱高/曲率规则不变。
- 六个SE檐口检查点，旧模型投影偏差18.97–27.51 px，新版0.43–5.50 px，均在6 px人工取点/
  原有轮廓取整容差内。这是局部设计图约束，不是测绘精度或完整轮廓验收。
- 顶层占位支承 **20处可生成、0处净空阻塞**；实际顶面全范围竖向间隙最小0.01999806 m，
  按3e-6 m浮点容差满足0.020 m规则。四角支承高约0.21737 m，未降柱、抬标高或缩小支承。
- 原18个场景几何摘要和v10文件保持不变；相对v10仅五层下檐及支承两网格变化，32个网格不变。
  拓扑检查非流形/绕向错误为0，各独立壳体体积为正；未证明全场景无交叉或结构连接成立。
- 正面、斜视、两张近景与平面叠图已检查。**M1仍revise**：屋面折痕/尖翘、构件真实形态未通过。
  50项建模测试通过；独立代码审查无可操作发现，但缺少LSP，未宣称正式静态审查批准。
  M2–M4未开始，游戏代码/导出/CPU-GPU验收未改变。
- v09未完成场景和诊断11保留；本次MCP操作成功。
- **仓库可移植场景仍是v08，不是v12**。v10/v12原始多场景文件和渲染仅在本机忽略目录；
  仓库只同步经过检查的JSON证据。新v12文件尚未做场景隔离复读，不宣称可移植性复验。

### 历史阻塞诊断：研究场景11（针对v10，不是外观白模v11）

v12已在保留该诊断的前提下修正来源比例错误；下列数值只描述v10，不是当前v12阻塞。

- 经 MCP 创建 `HHL_Corner_Clearance_Study_11`，读取实际 v10 三角面，四角最低点均在
  1.04 m 方形支承覆盖范围的角点，距柱心约 0.735 m。柱心 1 mm 探测约 38.3213 m，
  全范围最低为 37.7652 m；不能用中心射线代替全范围检查。
- 当前柱顶 37.750 m；占位规则要求净空 0.020 m、支承高严格大于 0.040 m，故屋面最低点
  必须高于 37.810 m，当前差约 44.8 mm。**这是估计几何/占位规则的冲突，不是真实建筑缺陷。**
- 图2-2-5的37.300 m为暗层标高，不能直接替代角柱柱顶尺寸。没有降柱、抬顶或缩小支承。
- 渲染后复验旧17个场景几何摘要及原v10文件哈希不变；四个最低点都在对应三角形内且满足
  平面方程。重复构建同版本被拒绝，未切换当前场景。49项建模测试通过。
- 诊断图使用41个1 mm方形探测点、非等比例坐标；不是精确剖面绘图或新的外观验收。
  M1仍revise。诊断场景/图片仅在本机；仓库同步数值证据，不新增可移植场景。

## 仓库携带的成果

[manifest.json](manifest.json) 列出需要随提交携带的文件、字节数和 SHA-256。

- [可移植 v08 白模](yellow-crane/exterior-whitebox-08-portable.blend)：约 0.93 MB，
  仅含生成的白模场景、网格、材质、相机和 World；不是原始多场景工作文件。
- [可移植性验证](yellow-crane/portability-verification.json)：通过 Blender MCP 从已保存的 v08
  隔离加载场景，提取依赖后重新加载导出文件，几何/变换/顶冠区域摘要一致。
  文件不包含图片、文本脚本、外部库、音频、视频、字体或驱动器；渲染路径改为相对路径。
  原 v08 文件、当前场景和原有 16 个场景的几何均保留。检查不覆盖每个 Blender 数据字段。
- [evidence](yellow-crane/evidence/)：v08 历史证据、v09 纯几何预检，以及v10/v12/v13/v14的拓扑、
  旧场景保留、基线网格比较、实际支承净空和视觉结论。JSON 记录不能替代新电脑上的 Blender 复验。
- [source-inventory.json](yellow-crane/source-inventory.json)：来源图号、原链接、原图及研究预览哈希，
  不含原图文件，不自动下载。来源版本/精度/使用边界仍以来源登记为准。

这份白模的开放边界仅是“不携带参考图文件”，不应推断原出版物获得开放许可，
也不应将研究几何标作测绘级或已完成第三方权利审查的商业交付物。

## 新电脑如何继续

在需要的提交已推送后 clone，并进入仓库根目录。可先离线验证交接包：

```sh
python3 -B -m unittest discover -s tools -p 'test_handoff_assets.py' -v
python3 -B -m unittest discover -s tools/blender -p 'test_*.py' -q
```

Windows 可以将 `python3` 换成对应的 `python`。测试需要 Git，不需要联网、Blender 或新 Python 依赖。
第一条核对文件哈希、忽略规则和提取证据；不冒充一次新的 Blender 几何验收。

### 加载白模

已在 Blender 5.2.1 LTS 验证，其他版本需要重新验证兼容性。
建议在干净的 Blender 文件中使用 **File → Append → 可移植文件 → Scene →
HHL_Exterior_Whitebox_08_Portable**，再切换至该 Scene。这是已验证的 scene-library 恢复方式，
不依赖原电脑的工作区布局。也可通过 Blender MCP 执行：

```python
from pathlib import Path
import bpy
root = Path('/path/to/phoenix-tower')  # 改为当前机器仓库绝对路径
name = 'HHL_Exterior_Whitebox_08_Portable'
if name in bpy.data.scenes:
    raise RuntimeError('已存在该检查点，请使用现有场景，勿重复加载')
path = root / 'docs/handoff/yellow-crane/exterior-whitebox-08-portable.blend'
with bpy.data.libraries.load(str(path), link=False) as (available, requested):
    if name not in available.scenes:
        raise RuntimeError('检查点场景缺失')
    requested.scenes = [name]
bpy.context.window.scene = requested.scenes[0]
```

将后续工作另存到本地 `.omx/references/yellow-crane/` 的新版本文件，
不要直接覆盖受版本管理的检查点。

### 下一步建模

1. 检查当前场景、磁盘剩余空间和 MCP 连接；勿复制旧运行状态冒充恢复成功。
2. 本机14已完成一/三层端点修正、有界几何复验及三机位视觉检查，仍须改形；不要重建或覆盖。后续改形使用新版本 **15**（若存在则递增），保留全部旧场景。
   先重载 `top_support_whitebox`、`crown_whitebox`，再导入/重载 `exterior_whitebox`。
   新电脑只有可移植08，需明确重建与本机14的区别。
3. 先用Blender实际loop triangles闭合三层16处交接风险复验：存储网格两种重三角化均有内部见证，
   加深分别约4.807/5.195 mm；旧方形检查的3.004 mm是另一方法，不能混用。不能靠降低柱高隐藏风险。
   屋面曲率/翼角连接仍须继续：结合2-2-13平面斜长与2-2-14真高，建立有来源的对应关系。
   保留一/三/五层已约束端点；五层轮廓维持自身图纸比例。先解决二/四层标注冲突，
   禁用“不作放样用”的虚线，不能直接用统一斜长倍率。37.300暗层标高仍不是已测柱顶。
4. 新版需检查旧场景、32个当前未动网格的范围回归、拓扑、支承全范围净空，并渲染正面/斜视/
   檐下近景。平面六点通过不可代替完整曲面、结构与视觉验收；M1通过之前不要游戏导出。

## 哪些不会随 clone 恢复

- 聊天全过程、内存中的 v09、当前窗口布局、临时权限状态和运行中的进程。
- `.omx/state/`、旧流水笔记、实验脚本、原始多场景 `.blend` 和备份。
- 原图、衍生预览、含原图的对比板：保留在旧机器的忽略目录；若需要图纸核验或重建参考板，
  必须在使用范围允许的前提下自行迁移或取得。结构研究脚本依赖清单中的预览文件，
  **不能仅靠 clone 重建私人参考板**。不要提交整个参考目录。
- 用户级 Codex 配置、技能/OMX 安装、Python、Blender 插件和凭据；参见环境说明。
- Git 仓库本地 `user.name` / `user.email` 配置；须在新 clone 中重新设置并核对。

## 维护规则

- `.omx` 默认忽略，仅白名单跟踪 `notepad.md` 和 `plans/*.md`。
  项目身份约束以 `AGENTS.md` 为准，不同步重复的机器专属 `project-memory.json`。
- 本次没有删除本地原图、旧成果或状态；旧笔记备份位于
  `.omx/state/notepad-before-context-portability-20260908.md`。
- 不将原始 `.blend` 直接拷入 Git。新检查点必须重复 scene-only 提取、依赖检查、复读比较，
  再更新清单；只保留有接手价值的检查点，避免把每次实验都变成永久二进制历史。
- 每次更新状态，区分“代码已实现 / 本次已验证 / 历史证据 / 尚未验证”；不要记“所有上下文已恢复”。
- 交接变更完成后提交并推送需要的分支；新电脑只能 clone 到远端已有的提交。
