# 历史执行计划

这里的计划与设计取舍随 Git 保存，供接手者理解约束；不表示自动恢复执行模式。
当前状态、已验证成果、下一步以 [项目交接入口](../../docs/handoff/README.md) 为准。

旧计划里的绝对路径、未提交/未 push 记录、工具故障和版本号是当时的记录。
新电脑应使用自己的仓库路径并重新检查 Git、Blender 与 MCP 状态。
这里仅允许人工审阅的 Markdown 计划；运行日志、凭据及图片不要放入本目录。
