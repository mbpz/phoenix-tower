# 四角净空诊断：先定位，不以猜测修改建筑

已提交 v10 证据（2477de7）。本轮目标是完成下一项阻塞诊断，而非声称四角构造已修复。

1. 保留现有 v10/全部旧场景、柱高、屋顶及支承保护阈值。
2. 先增加裁剪最低点见证测试，锁定 `underside_cap` 现有行为及四角阻断回归。
3. 将现有裁剪计算提取为返回最低点/来源三角面的探测，旧 API 复用同一计算；不复制裁剪逻辑。
4. 通过 Blender MCP 读取实际网格，输出四角最低点、柱顶与所需净空；生成独立剖线研究场景和渲染。
5. 剖线仅是计算诊断；37.300 m 是图纸暗层标高，不能代替尚未证实的柱顶尺寸。
6. 复验旧场景摘要与全部测试，更新交接/证据哈希。原图、诊断图片、多场景 blend 继续忽略。

验收：高度旧结果不变；见证点在支承方形内、在对应三角面上；实际 Blender 四角冲突均能定位；
不修改原模型；四角未解决与真实建筑尺寸未确认继续列为风险。
